// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
<<<<<<< HEAD
  url      = "mysql://root:password@localhost:3306/visionflow"
=======
  url      = "mysql://root:@localhost:3306/thegoldenship"
>>>>>>> 9cac60f794ea94721b8996abe68451aa402b21e6
}

model Task {
  id           String  @id @default(cuid())
  title        String
  content      String?
  parentTask   Task?   @relation(name: "SubTasks", fields: [parentTaskId], references: [id])
  parentTaskId String?
  subTasks     Task[]  @relation(name: "SubTasks")
  createdAt DateTime @default(now()) @map(name: "created_at")
  updatedAt DateTime @updatedAt @map(name: "updated_at")
  published Boolean  @default(false)
  status    String?  @default("pending")
  author    User?    @relation(fields: [authorId], references: [id])
  authorId  String?
}

model User {
  id        String   @id @default(cuid())
  name      String?
  email     String?  @unique
  password  String?
  provider  String?  @default("local")
  providerId String?
  role     String?  @default("student")
  createdAt DateTime @default(now()) @map(name: "created_at")
  updatedAt DateTime @updatedAt @map(name: "updated_at")
  tasks     Task[]

}
